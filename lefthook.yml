pre-commit:
  commands:
    lint:
      glob: "*.{js,ts,svelte,css,json}"
      run: npm run lint:fix
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}
