services:
  claude:
    build:
      context: .
      dockerfile: Dockerfile
    stdin_open: true
    tty: true
    depends_on:
      - mongodb
    environment:
      - MONGO_URL=mongodb://mongodb:27017
  mongodb:
    image: mongo:8
    restart: unless-stopped
